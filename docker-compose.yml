version: "3.9"

services:
  tool:
    privileged: true
    build: .
    environment:
      DNS_SERVER: "1.1.1.1"
    volumes:
      - "./regions.development.yml:/var/app/regions.yml"
    ports:
      - "8080:8080"
